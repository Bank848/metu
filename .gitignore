node_modules
.next
dist
build
coverage
.cache

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# editor / dev tooling
.vscode/*
!.vscode/extensions.json
.idea
.cursor/
.aider*
.continue/
.claude/

# OS
.DS_Store
Thumbs.db

# prisma
packages/db/prisma/dev.db
packages/db/prisma/dev.db-journal

# tests — Playwright + Vitest output
test-results
playwright-report
blob-report
.last-run.json
apps/web/coverage

# tsc incremental build cache
*.tsbuildinfo

# QA workflow artefacts (reports/ + shots/) — per-run output kept on
# disk for audit but not committed. Anchor with leading slash so we
# don't accidentally ignore the /admin/reports API route (whose
# folder happens to be named "reports").
/reports/
shots/

# local workspace + scratch (per-developer notes, agent configs, scratchpads)
.scratch/
.notes/
.workspace/
AGENTS.md
GEMINI.md
CLAUDE.md
.cursorrules
graphify-out/
.supabase-credentials.local
.google-oauth-credentials.local
.stripe-credentials.local
.resend-credentials.local
